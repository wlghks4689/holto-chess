# 승인 결정과 변경 이력

## BAL-004-APPROVAL — 낮은 랭크 카드 가격 인하

- 상태: 사용자 승인. 별도 작업에서 “랭크 2,3,4 가격인하를 실행하세요”라고 요청했고, 이번 요청에서 현지화 변경과 함께 커밋·푸시·운영 배포를 명시함.
- 결정: rank 2=2BB, rank 3=3BB, rank 4=4BB. rank 5=5BB 및 나머지 가격·경제 규칙은 유지.
- 출처: `codex/balance-latest`의 구현 커밋 `7700060bf40d89c729aee4d3c97f0a56e0bebfe8`, 승인 기록 `c7b2315`. 분석 브랜치 전체를 통합하지 않고 현재 `main`의 `BALANCE.rankPrices`와 관련 회귀 테스트에만 반영.
- 통합 검증: `npm test -- --run` 463/463, `npm run test:workers` 13/13, `npm run lint`, `npm run build`, `npx wrangler deploy --dry-run` 통과. Worker 테스트의 로컬 로그 쓰기 경고는 테스트 실패가 아님.
- 잔여 위험: BAL-004 분석에서 구매율 상승은 확인했으나 최종 성과 향상은 확인되지 않음. 운영에서 낮은 랭크 구매율·종료 BB·후속 라운드 성과를 관찰.

## MP-2026-09-24 — 멀티플레이 공개·종료·관전 경계

- 상태: 사용자 직접 승인. MP-01~07의 권고 전체 처리 요청. 기록은 R5 공통 연출이 끝나고 게임 참가자가 최종 순위표를 확인한 시점 전에는 열람할 수 없어야 함. 참가자 일정과 관전자 일정을 분리하며 실제 긴 타이브레이크의 대기는 유지함.
- 결정: `finalResultsReleasedAt` 서버 선언 분리, 단계별 공개 projection, RTT 보정 및 불확실성 경고, 온라인 상점 필수 정보 표시, 연결·관전 상태의 지속 표시. 점수·보상·경제 수치 변경 없음.
- 종료 확인: 공통 endsAt 이후 활성 인간 결승 참가자의 보이는 순위표 진입을 확인. 인간 결승 참가자가 전부 탈락/이탈했으면 관전자의 완료 확인 허용. 단말 전체 ACK를 기다리는 정책은 아님.
- 기준/보고서: `afbad6c` (`b56260f` 제품 기준), `qa/2026-09-24-multiplayer-ux-audit.md`는 로컬 보고서이며 원본 보고서 커밋 SHA 미정. 구현 설명·검증은 `qa/2026-09-24-multiplayer-remediation.md`.
- 구현 커밋: `9233d51` 단계별 공개·최종 기록 잠금, `666279e` 튜토리얼 쇼다운 체크포인트 정렬, `8155529` 증강 제거 뒤 남은 튜토리얼 테스트 분기 제거. 최신 `main` 통합 커밋으로 push 완료.
- 운영 배포: Cloudflare Worker `porena`, Version ID `ac1c7348-0c50-49e8-ae8f-fd808f4b2f47`. `https://porena.kr/api/health` HTTP 200 및 운영 HTML의 `index-DUXwIxAK.js` 자산 확인.
- 잔여 위험: 비대칭 회선의 완전 동시성 보장 불가, 구형 탭 전환·실기기·동시 방 부하 별도 검증. MP-08(P3)은 이번 중간 이상 승인과 구분.

## GAME-2026-09-24 — 증강 규칙 전체 제거

- 상태: 사용자 직접 승인. 첨부 지시에서 UI만이 아닌 게임 규칙·데이터·효과·프로토콜·튜토리얼·시뮬레이터 전면 제거를 요청하고, 커밋·push·배포까지 명시함.
- 결정: R2/R4 결과 뒤 선택 단계를 없애고 다음 라운드로 진행. 카드 가격·판매율·리롤 비용·승리 보상·최종 총점은 기존 기본 수치만 적용. 다른 라운드 규칙이나 밸런스 수치는 변경하지 않음.
- 기존 방: 저장된 증강 필드와 선택 단계만 마이그레이션. 획득된 승점·BB·카드와 경기 결과는 보존하며 과거 보상을 소급 재계산하지 않음.
- BAL-001 영향: 기존 REQUEST의 증강 항목과 `augments.ts` 참조는 폐지된 규칙의 역사 기록이며 현행 분석 범위에서 제외해야 함. 원본 REQUEST는 변경하지 않음. 후속 분석은 이 결정의 구현 커밋을 새 기준으로 재설정해야 함.
- 검증: `npm test` 403/403, `npm run test:workers` 12/12, `npm run lint`, `npm run build` 통과. 밸런스 시뮬레이터 전용 테스트 2/6 통과·4/6 실패는 기존 BAL-001에서 기록한 R2 `SHOP`/`DRAFT_ORDER` 불일치이며 이 변경에서 새로 발생하지 않음.
- 구현 커밋: `720f7dc` (`codex/release-audit`, `origin/codex/release-audit` 푸시 확인).
- 운영 배포: Cloudflare Worker `porena`, Version ID `1b1b1147-3fe9-44f3-b82b-1b451dce4fb8`. `https://porena.kr/api/health` 정상, 운영 HTML의 JS/CSS 해시가 최종 빌드와 일치함.

## COLLAB-001 — 역할 및 승인 경계

- 상태: 사용자 승인됨(2026-09-24 현재 작업 요청).
- 근거: 사용자가 역할 분업, Codex 통합 책임, Claude 분석 전용 권한, 규칙·밸런스 변경 사전 승인을 직접 지정함.
- 결정: 세부 계약은 [README](README.md)를 따른다. 이번 범위는 협업 문서와 첫 의뢰 준비이며 게임 변경은 하지 않는다.
- 원본 분석 보고서 / 보고서 SHA: 해당 없음(사용자 직접 결정).
- 게임 구현 커밋: 해당 없음. 문서 커밋: 미정.
- 검증: 저장소·브랜치·원격·기존 자료 조사 완료. 인계 공유는 미완료.
- 문서 공유 승인: 2026-09-24 사용자 “네 커밋 푸쉬하세요”. 이번 협업 문서만 대상이며 다른 작업의 변경·게임 구현·배포는 포함하지 않는다. 실제 문서 SHA는 커밋 후 인계 메시지에 기록한다.

## BAL-001 — 진단 의뢰 준비(밸런스 변경 승인 아님)

- 상태: Codex가 진단 의뢰 초안 준비. Claude 미착수.
- 이유: 기존 시뮬레이터가 현재 R2 DRAFT_ORDER에서 중단되므로 현재 버전의 전체 경기 지표를 먼저 신뢰할 수 있는지 확인해야 함.
- 측정 근거: 2026-09-24 전용 테스트 6개 중 2개 통과·4개 실패. 상세는 REQUEST의 증거 기록.
- 분석 결과 / 사용자 수치 승인 / 구현 SHA / 구현 검증: 아직 없음.

## 발견한 불일치 — 결정이 아닌 검토 대기 기록

| ID | 기존 자료 | 현재 구현 근거 | 처리 |
| --- | --- | --- | --- |
| DIFF-001 | `holto-chess/README.md` R2 출전 2장 선택, 자동 진행 미포함 설명 | `src/game/engine.ts` rulesVersion=2 앵커+보조 2개 Split Run; `src/game/room.ts` 자동 진행 | 문서 잔존 가능성. 수정하지 않음 |
| DIFF-002 | `SHOWDOWN_CINEMATIC_PROGRESS.md` 사용자 승인 임시 R5 6/4/2/0 | `src/game/config.ts` 현재 R5 20/12/5/3 | 과거 승인 기록임을 보존. 현재 수치의 최종 승인 이력은 이번 조사에서 확정하지 않음 |
| DIFF-003 | 양쪽 `IMPLEMENTATION_REPORT.md` 과거 임시 족보 점수표 | `src/game/config.ts` 현재 HIGH_CARD 0부터 ROYAL_FLUSH 40까지 별도 표 | 현재 구현과 역사 기록을 구분. 점수 재산정 금지 |
| DIFF-004 | 시뮬레이터 R2 종료 6명·R2 브래킷 집계 | 현재 R2 무탈락, R3 Omaha Swiss 후 6명 | 실제 실행도 R2 phase 불일치로 실패. 도구 개선안 요청 |
| DIFF-005 | 루트 README Game Flow는 R3·R4 누적 승점 하위 탈락으로 일반화 | 같은 README 라운드 표 및 `engine.ts` R4 생존조 패자 탈락 | R3 누적 승점 탈락과 R4 브래킷 탈락 구분 필요. 임의 변경 없음 |

표의 `src/` 경로는 `holto-chess/` 기준. 소스 기준 동작 기록은 승인된 새 기획을 뜻하지 않는다.

## 이후 결정 기록 양식

결정 ID / 상태 / 제안별 채택·보류·기각 이유 / 사용자 승인 일시와 메시지 근거 /
분석 BASE_COMMIT / 보고서 커밋 SHA·경로 / 현재 개발 버전과 관련 diff /
재검증 필요 여부·결과 / Codex 구현 커밋 SHA / 테스트 명령·결과 / 잔여 위험.
