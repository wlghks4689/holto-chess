# Project runtime

- The canonical application is the Cloudflare app in `holto-chess/`.
- When asked to start or open the development server, run `npm run dev -- --host 0.0.0.0` from the repository root. The root script delegates to `holto-chess/`.
- Do not start the legacy root Vite application directly.
